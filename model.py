# from keras.models import load_model
# import os

# MODEL_PATH = os.path.join(os.path.dirname(__file__), 'waste_classification_model_final.keras')
# waste_classification_model = load_model(MODEL_PATH)